# 2026-04-10

- `/reviews` 一覧カードの purchaseLinks 描画を先頭 1 件固定から複数件描画へ変更した。`ひゃくえむ。` は frontmatter に上巻・下巻の 2 件が既に入っており、`src/pages/reviews.astro` 側の `purchaseLinks[0]` が原因だった。`npm run typecheck`、`npm run build`、`npm run verify:frontend`、Claude review gate `ok: true` を確認。
