# 2026-04-13

- レビューあり gallery 詳細ページ 5件（死ぬ瞬間の5つの後悔、楽園の楽園、ひゃくえむ。、列、青天）に `note` フィールドを追加。Hero（description = 作品紹介）と What Lingers（note = 読後の感覚）のテキスト重複を解消。`autobiography-buffon` を基準実装とした横展開。lint / typecheck / build / e2e 14テスト全通過、`npm run verify:frontend` OK。Codex review gate ok:true。PR #88 squash merge 済み（`54cf043`）。
- review detail page 6件（`buffon-autobiography`、`hyakuemu`、`rakuen-no-rakuen`、`retsu`、`seiten`、`top-five-regrets-of-the-dying`）に `readingCompass` を追加。`src/pages/reviews/[slug].astro` で Reading Compass から `recommendedFor` の再表示を外し、「読書前の視点」と「おすすめ対象」を役割分離。`buffon-autobiography` と `seiten` は本文側の読者対象重複も整理。`npm run lint` / `npm run typecheck` / `npm run build` / `npm run verify:frontend` 成功、Playwright 14 passed、Claude review gate arch / diff とも `ok: true`。
